# Food Roulette backend — Phase 1
# Python 3.11+ recommended.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Chat (M2). Only imported when GEMINI_API_KEY is set — /chat degrades to the
# plain recommendation path without it, so this is not required to boot.
google-genai==2.8.0

pyjwt
argon2-cffi
email-validator