# Test-only dependencies. Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
httpx==0.28.1        # fastapi.testclient is built on it
pyyaml==6.0.2        # openapi.yaml generation (tools/make_contract.py)
