# GENERATED by backend/tools/make_contract.py — do not hand-edit.
# Change the pydantic models in backend/app/schemas.py and re-run it.
openapi: 3.1.0
info:
  title: Food Roulette API
  description: 'Contract for the Flutter client. Frozen per ROADMAP §3 — additive
    changes are safe, renames are not.


    Every non-2xx response uses the ErrorEnvelope shape.'
  version: 0.1.0
paths:
  /health:
    get:
      summary: Health
      description: Liveness + a quick row count so you can confirm ingest ran.
      operationId: health_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                title: Response Health Health Get
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /restaurants:
    get:
      summary: List Restaurants
      description: All restaurants with a count of their menu items (smoke-test endpoint).
      operationId: list_restaurants_restaurants_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  type: object
                type: array
                title: Response List Restaurants Restaurants Get
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /restaurants/{restaurant_id}:
    get:
      summary: Get Restaurant
      description: 'A restaurant''s detail-screen payload: menu, safe-dish list, and
        cached

        Google Places enrichment (lazy; nulls when not keyed).'
      operationId: get_restaurant_restaurants__restaurant_id__get
      parameters:
      - name: restaurant_id
        in: path
        required: true
        schema:
          type: string
          title: Restaurant Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestaurantDetailOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /restaurants/{restaurant_id}/photos/{photo_index}:
    get:
      summary: Restaurant Photo
      description: 'Proxy one of a restaurant''s cached Place photos *through* the
        backend.


        The only Place handle the client ever holds is this keyless URL; the real

        Places key is added here, server-side, and so never leaves the process

        (DESIGN §7).'
      operationId: restaurant_photo_restaurants__restaurant_id__photos__photo_index__get
      parameters:
      - name: restaurant_id
        in: path
        required: true
        schema:
          type: string
          title: Restaurant Id
      - name: photo_index
        in: path
        required: true
        schema:
          type: integer
          title: Photo Index
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /history:
    post:
      summary: Write History
      description: 'Persist a batch of user feedback/impression events.


        The endpoint is intentionally fire-and-forget friendly: the whole batch is

        committed atomically, and no event is allowed to alter the safety filter.

        Identity comes from the bearer token, never the body.'
      operationId: write_history_history_post
      security:
      - HTTPBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HistoryBatchIn'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HistoryWriteOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    get:
      summary: Get History
      description: Return the authenticated user's newest history events first.
      operationId: get_history_history_get
      security:
      - HTTPBearer: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HistoryOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /history/stats:
    get:
      summary: Get History Stats
      description: Return aggregated history statistics for the authenticated user.
      operationId: get_history_stats_history_stats_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HistoryStatsOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      security:
      - HTTPBearer: []
  /auth/register:
    post:
      summary: Register
      operationId: register_auth_register_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterIn'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /auth/login:
    post:
      summary: Login
      operationId: login_auth_login_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginIn'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /preferences:
    get:
      summary: Get Preferences
      description: 'Look up the authenticated user''s saved allergy/diet settings.
        If they''ve

        never saved any, hand back empty defaults instead of an error. Identity

        comes from the bearer token, never the path or body.'
      operationId: get_preferences_preferences_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      security:
      - HTTPBearer: []
    put:
      summary: Put Preferences
      description: 'Save (or overwrite) the authenticated user''s allergy/diet settings.

        Identity comes from the bearer token, never the path or body.'
      operationId: put_preferences_preferences_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PreferencesIn'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferencesOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      security:
      - HTTPBearer: []
  /recommend:
    post:
      summary: Recommend
      description: 'Pick restaurants for this user. First we throw out every dish
        that

        breaks a hard rule (allergy, halal, etc.) — no exceptions. Then, among

        what''s left, we sort by how well it matches soft preferences like spice

        level and distance. Identity comes from the bearer token, never the body.'
      operationId: recommend_recommend_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecommendIn'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /chat:
    post:
      summary: Chat
      description: 'Read the message, filter, then describe what survived (DESIGN
        §6).


        Gemini sits on both ends and never in the middle: it turns the message into

        preferences, and later writes the reply, but the choice of restaurants is

        made by the same filter /recommend uses. If Gemini is unreachable the

        request still succeeds — the user gets their saved-profile recommendations

        and a `degraded` code explaining why the reply is plain. Identity comes

        from the bearer token, never the body.'
      operationId: chat_chat_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatIn'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatOut'
        '422':
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        default:
          description: Error envelope (ROADMAP §3.2)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      security:
      - HTTPBearer: []
components:
  schemas:
    ChatIn:
      properties:
        text:
          type: string
          title: Text
        session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Id
        latitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Latitude
        longitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Longitude
        limit:
          type: integer
          maximum: 50.0
          minimum: 1.0
          title: Limit
          default: 10
        seed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Seed
      type: object
      required:
      - text
      title: ChatIn
    ChatOut:
      properties:
        reply:
          type: string
          title: Reply
        recommendations:
          items:
            $ref: '#/components/schemas/RecommendedRestaurant'
          type: array
          title: Recommendations
        session_id:
          type: string
          title: Session Id
        fallback_used:
          type: boolean
          title: Fallback Used
          default: false
        degraded:
          anyOf:
          - type: string
            enum:
            - NO_RESULTS
            - INVALID_PREFS
            - NOT_FOUND
            - UNAUTHORIZED
            - LLM_UNAVAILABLE
            - UPSTREAM_TIMEOUT
            - INTERNAL
          - type: 'null'
          title: Degraded
      type: object
      required:
      - reply
      - recommendations
      - session_id
      title: ChatOut
      description: 'Same cards as /recommend, plus the prose that introduces them.


        The client renders cards from `recommendations` and never by reading

        `reply` — that separation is what stops a chatty model from inventing a

        restaurant (DESIGN §6).'
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HardConstraints:
      properties:
        allergens:
          items:
            type: string
            enum:
            - peanuts
            - tree_nuts
            - shellfish
            - fish
            - wheat
            - soy
            - milk
            - eggs
            - sesame
          type: array
          title: Allergens
          default: []
        halal:
          type: boolean
          title: Halal
          default: false
        no_beef:
          type: boolean
          title: No Beef
          default: false
        no_pork:
          type: boolean
          title: No Pork
          default: false
        vegetarian:
          type: boolean
          title: Vegetarian
          default: false
        vegan:
          type: boolean
          title: Vegan
          default: false
        jain:
          type: boolean
          title: Jain
          default: false
        jay:
          type: boolean
          title: Jay
          default: false
        celiac:
          type: boolean
          title: Celiac
          default: false
      type: object
      title: HardConstraints
      description: 'Non-negotiable rules. Every one of these is enforced in SQL by

        `filter.py` — the LLM never gets a say (DESIGN §4).'
    HistoryBatchIn:
      properties:
        events:
          items:
            $ref: '#/components/schemas/HistoryEvent'
          type: array
          title: Events
      type: object
      required:
      - events
      title: HistoryBatchIn
    HistoryEvent:
      properties:
        session_id:
          type: string
          title: Session Id
        restaurant_id:
          type: string
          title: Restaurant Id
        action_type:
          type: string
          enum:
          - IMPRESSION
          - CLICK
          - SPIN
          - REJECTION
          title: Action Type
        context:
          anyOf:
          - type: object
          - type: 'null'
          title: Context
      type: object
      required:
      - session_id
      - restaurant_id
      - action_type
      title: HistoryEvent
    HistoryEventOut:
      properties:
        id:
          type: integer
          title: Id
        session_id:
          type: string
          title: Session Id
        user_id:
          type: string
          title: User Id
        restaurant_id:
          type: string
          title: Restaurant Id
        action_type:
          type: string
          enum:
          - IMPRESSION
          - CLICK
          - SPIN
          - REJECTION
          title: Action Type
        ts:
          type: string
          title: Ts
        context:
          anyOf:
          - type: object
          - type: 'null'
          title: Context
      type: object
      required:
      - id
      - session_id
      - user_id
      - restaurant_id
      - action_type
      - ts
      title: HistoryEventOut
    HistoryOut:
      properties:
        history:
          items:
            $ref: '#/components/schemas/HistoryEventOut'
          type: array
          title: History
      type: object
      required:
      - history
      title: HistoryOut
    HistoryRestaurantStats:
      properties:
        restaurant_id:
          type: string
          title: Restaurant Id
        impressions:
          type: integer
          title: Impressions
        clicks:
          type: integer
          title: Clicks
        rejections:
          type: integer
          title: Rejections
        spins:
          type: integer
          title: Spins
        click_through_rate:
          type: number
          title: Click Through Rate
      type: object
      required:
      - restaurant_id
      - impressions
      - clicks
      - rejections
      - spins
      - click_through_rate
      title: HistoryRestaurantStats
    HistoryStatsOut:
      properties:
        user_id:
          type: string
          title: User Id
        totals:
          $ref: '#/components/schemas/HistoryStatsTotals'
        click_through_rate:
          type: number
          title: Click Through Rate
        restaurants:
          items:
            $ref: '#/components/schemas/HistoryRestaurantStats'
          type: array
          title: Restaurants
      type: object
      required:
      - user_id
      - totals
      - click_through_rate
      - restaurants
      title: HistoryStatsOut
    HistoryStatsTotals:
      properties:
        impressions:
          type: integer
          title: Impressions
        clicks:
          type: integer
          title: Clicks
        rejections:
          type: integer
          title: Rejections
        spins:
          type: integer
          title: Spins
      type: object
      required:
      - impressions
      - clicks
      - rejections
      - spins
      title: HistoryStatsTotals
    HistoryWriteOut:
      properties:
        accepted:
          type: integer
          title: Accepted
      type: object
      required:
      - accepted
      title: HistoryWriteOut
    LoginIn:
      properties:
        email:
          type: string
          format: email
          title: Email
        password:
          type: string
          title: Password
      type: object
      required:
      - email
      - password
      title: LoginIn
    PlaceReview:
      properties:
        author_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Author Name
        rating:
          anyOf:
          - type: number
          - type: 'null'
          title: Rating
        text:
          anyOf:
          - type: string
          - type: 'null'
          title: Text
        relative_time:
          anyOf:
          - type: string
          - type: 'null'
          title: Relative Time
      type: object
      title: PlaceReview
    PlacesEnrichment:
      properties:
        rating:
          anyOf:
          - type: number
          - type: 'null'
          title: Rating
        user_rating_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: User Rating Count
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        formatted_address:
          anyOf:
          - type: string
          - type: 'null'
          title: Formatted Address
        photos:
          items:
            type: string
          type: array
          title: Photos
          default: []
        opening_hours:
          items:
            type: string
          type: array
          title: Opening Hours
          default: []
        reviews:
          items:
            $ref: '#/components/schemas/PlaceReview'
          type: array
          title: Reviews
          default: []
        synced_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Synced At
      type: object
      title: PlacesEnrichment
    PreferencesIn:
      properties:
        hard:
          $ref: '#/components/schemas/HardConstraints'
          default:
            allergens: []
            halal: false
            no_beef: false
            no_pork: false
            vegetarian: false
            vegan: false
            jain: false
            jay: false
            celiac: false
        soft:
          $ref: '#/components/schemas/SoftPreferences'
          default:
            diet_style: none
            wants_parking: false
      type: object
      title: PreferencesIn
    PreferencesOut:
      properties:
        hard:
          $ref: '#/components/schemas/HardConstraints'
          default:
            allergens: []
            halal: false
            no_beef: false
            no_pork: false
            vegetarian: false
            vegan: false
            jain: false
            jay: false
            celiac: false
        soft:
          $ref: '#/components/schemas/SoftPreferences'
          default:
            diet_style: none
            wants_parking: false
        user_id:
          type: string
          title: User Id
      type: object
      required:
      - user_id
      title: PreferencesOut
    RecommendIn:
      properties:
        latitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Latitude
        longitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Longitude
        limit:
          type: integer
          maximum: 50.0
          minimum: 1.0
          title: Limit
          default: 10
        seed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Seed
      type: object
      title: RecommendIn
    RecommendOut:
      properties:
        recommendations:
          items:
            $ref: '#/components/schemas/RecommendedRestaurant'
          type: array
          title: Recommendations
        fallback_used:
          type: boolean
          title: Fallback Used
      type: object
      required:
      - recommendations
      - fallback_used
      title: RecommendOut
    RecommendedRestaurant:
      properties:
        restaurant_id:
          type: string
          title: Restaurant Id
        name_th:
          anyOf:
          - type: string
          - type: 'null'
          title: Name Th
        name_en:
          anyOf:
          - type: string
          - type: 'null'
          title: Name En
        latitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Latitude
        longitude:
          anyOf:
          - type: number
          - type: 'null'
          title: Longitude
        distance_m:
          anyOf:
          - type: number
          - type: 'null'
          title: Distance M
        price_tier:
          anyOf:
          - type: string
          - type: 'null'
          title: Price Tier
        rating:
          anyOf:
          - type: number
          - type: 'null'
          title: Rating
        user_rating_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: User Rating Count
        photo_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Photo Url
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        safety_tier:
          type: string
          enum:
          - verified
          - unverified
          title: Safety Tier
        needs_ack:
          type: boolean
          title: Needs Ack
        ack_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Ack Reason
        excluded_count:
          type: integer
          title: Excluded Count
        safe_dishes:
          items:
            $ref: '#/components/schemas/SafeDish'
          type: array
          title: Safe Dishes
      type: object
      required:
      - restaurant_id
      - name_th
      - name_en
      - latitude
      - longitude
      - distance_m
      - price_tier
      - safety_tier
      - needs_ack
      - excluded_count
      - safe_dishes
      title: RecommendedRestaurant
      description: 'The single object the whole UI is built from (ROADMAP §3.1).


        `rating` / `user_rating_count` / `photo_url` come from Google Places

        (M4, lazy + cached server-side — see places.py); they stay null when the

        venue is un-keyed or the lookup failed, and the client must render

        gracefully without them.


        `description` is the human-written, searchable blurb (display only — it is

        never read by filter.py, so it can never become a safety claim).'
    RegisterIn:
      properties:
        email:
          type: string
          format: email
          title: Email
        password:
          type: string
          title: Password
        display_name:
          type: string
          title: Display Name
      type: object
      required:
      - email
      - password
      - display_name
      title: RegisterIn
    RestaurantDetailOut:
      properties:
        restaurant:
          type: object
          title: Restaurant
        menu:
          items:
            type: object
          type: array
          title: Menu
        safe_dishes:
          items:
            $ref: '#/components/schemas/SafeDish'
          type: array
          title: Safe Dishes
        places:
          $ref: '#/components/schemas/PlacesEnrichment'
      type: object
      required:
      - restaurant
      - menu
      - safe_dishes
      - places
      title: RestaurantDetailOut
    SafeDish:
      properties:
        id:
          type: integer
          title: Id
        name_th:
          anyOf:
          - type: string
          - type: 'null'
          title: Name Th
        name_en:
          anyOf:
          - type: string
          - type: 'null'
          title: Name En
        price_thb:
          anyOf:
          - type: number
          - type: 'null'
          title: Price Thb
        spicy_level:
          anyOf:
          - type: integer
          - type: 'null'
          title: Spicy Level
        safety_tier:
          type: string
          enum:
          - verified
          - unverified
          title: Safety Tier
      type: object
      required:
      - id
      - name_th
      - name_en
      - price_thb
      - spicy_level
      - safety_tier
      title: SafeDish
    SoftPreferences:
      properties:
        spicy_tolerance:
          anyOf:
          - type: integer
            maximum: 3.0
            minimum: 0.0
          - type: 'null'
          title: Spicy Tolerance
        price_tier:
          anyOf:
          - type: string
            enum:
            - $
            - $$
            - $$$
            - $$$$
          - type: 'null'
          title: Price Tier
        diet_style:
          type: string
          enum:
          - keto
          - low_carb
          - mediterranean
          - carnivore
          - none
          title: Diet Style
          default: none
        wants_parking:
          type: boolean
          title: Wants Parking
          default: false
      type: object
      title: SoftPreferences
    TokenOut:
      properties:
        access_token:
          type: string
          title: Access Token
        token_type:
          type: string
          title: Token Type
        user_id:
          type: string
          title: User Id
      type: object
      required:
      - access_token
      - token_type
      - user_id
      title: TokenOut
    UserOut:
      properties:
        id:
          type: string
          title: Id
        email:
          type: string
          title: Email
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        created_at:
          type: string
          title: Created At
      type: object
      required:
      - id
      - email
      - display_name
      - created_at
      title: UserOut
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    ErrorBody:
      properties:
        code:
          enum:
          - NO_RESULTS
          - INVALID_PREFS
          - NOT_FOUND
          - UNAUTHORIZED
          - LLM_UNAVAILABLE
          - UPSTREAM_TIMEOUT
          - INTERNAL
          title: Code
          type: string
        message:
          title: Message
          type: string
        detail:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Detail
      required:
      - code
      - message
      title: ErrorBody
      type: object
    ErrorEnvelope:
      $defs:
        ErrorBody:
          properties:
            code:
              enum:
              - NO_RESULTS
              - INVALID_PREFS
              - NOT_FOUND
              - UNAUTHORIZED
              - LLM_UNAVAILABLE
              - UPSTREAM_TIMEOUT
              - INTERNAL
              title: Code
              type: string
            message:
              title: Message
              type: string
            detail:
              anyOf:
              - type: string
              - type: 'null'
              default: null
              title: Detail
          required:
          - code
          - message
          title: ErrorBody
          type: object
      properties:
        error:
          $ref: '#/components/schemas/ErrorBody'
      required:
      - error
      title: ErrorEnvelope
      type: object
  securitySchemes:
    HTTPBearer:
      type: http
      scheme: bearer
